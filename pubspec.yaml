name: parking_jjsolarte
description: A Flutter project by Jeison Solarte

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.11.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  flutter_bloc: ^7.0.1
  equatable: ^2.0.2
  liquid_swipe: ^2.1.0

  firebase_core: "^1.3.0"
  firebase_auth: "^1.4.1"
  firebase_database: "^7.1.1"


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/icon/